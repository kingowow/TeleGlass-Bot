# 💎 ربات تلگرام دکمه شیشه‌ای (TeleGlass)

یک ربات تلگرامی سبک و بدون سرور که بر روی پلتفرم Cloudflare Workers اجرا می‌شود. این ربات به کاربران اجازه می‌دهد به سادگی دکمه‌های شیشه‌ای (inline keyboard) به هر پیامی اضافه کرده و آن را به کانال یا کاربر دیگری ارسال کنند.

---

## ✨ ویژگی‌ها

-   **✍️ ساخت آسان پیام**: هر نوع پیامی (متن، عکس، ویدیو و...) را به ربات بفرستید.
-   **🖱️ سازنده دکمه داخلی**: به سادگی با ریپلای روی پیام خود، دکمه‌ها را بسازید.
-   **🚀 بدون سرور (Serverless)**: کاملاً بر روی سرویس رایگان Cloudflare اجرا می‌شود. بدون نیاز به سرور و دردسرهای مدیریت آن.
-   **📢 ارسال به مقصدهای مختلف**: پیام نهایی را به هر کانال، گروه یا کاربری ارسال کنید.
-   **👥 چندکاربره**: چندین کاربر می‌توانند به صورت همزمان و بدون تداخل از ربات استفاده کنند.
-   **🔒 امن**: سبک، بهینه و طراحی شده با تمرکز بر امنیت.

---

## ⚙️ چطور کار می‌کند؟

روند کار بسیار ساده و روان طراحی شده است:

1.  **ارسال محتوا**: شما محتوای اصلی پیام (مانند یک متن، عکس یا ویدیو) را مستقیماً به ربات ارسال می‌کنید.
2.  **ریپلای با تنظیمات**: سپس روی آن پیام ریپلای کرده و با یک فرمت خاص، تنظیمات را ارسال می‌کنید: خط اول شناسه چت مقصد (`@your_channel` یا یک آیدی عددی) است و خطوط بعدی، دکمه‌های شما را تعریف می‌کنند.

---

## 🚀 نصب و راه‌اندازی

برای راه‌اندازی ربات در چند دقیقه، مراحل زیر را دنبال کنید.

### ۱. دریافت توکن ربات تلگرام

-   در تلگرام به ربات [@BotFather](https://t.me/BotFather) پیام دهید.
-   با استفاده از دستور `/newbot` یک ربات جدید بسازید.
-   BotFather یک توکن منحصر به فرد به شما می‌دهد. آن را در جایی امن نگهداری کنید.

### ۲. دریافت کد

-   کد موجود در فایل `worker.js` این مخزن را کپی کنید.

### ۳. استقرار در Cloudflare Workers

-   وارد داشبورد Cloudflare خود شوید.
-   به بخش **Workers & Pages** بروید و یک **Worker** جدید بسازید.
-   یک نام برای آن انتخاب کنید (مثلاً `telegram-button-bot`).
-   کد `worker.js` را در ویرایشگر آنلاین Cloudflare کپی کنید.

### ۴. تنظیم توکن ربات

اسکریپت برای کار کردن به توکن ربات شما نیاز دارد. این خط را در بالای اسکریپت `worker.js` پیدا کرده و توکن خود را جایگزین کنید:
```javascript
const BOT_TOKEN = "YOUR_BOT_TOKEN_HERE";
```
مقدار `"YOUR_BOT_TOKEN_HERE"` را با توکنی که از BotFather دریافت کردید، جایگزین کنید.

*(برای امنیت بیشتر، می‌توانید به جای نوشتن توکن در کد، از متغیرهای محیطی (secrets) در Cloudflare استفاده کنید که البته نیازمند تغییر کوچکی در کد است.)*

### ۵. استقرار (Deploy)

-   روی دکمه **Save and Deploy** کلیک کنید. Cloudflare یک URL منحصر به فرد به شما می‌دهد (مانند `https://telegram-button-bot.your-name.workers.dev`).

### ۶. تنظیم Webhook

-   در نهایت، به تلگرام بگویید که آپدیت‌ها را به کجا ارسال کند. URL زیر را در مرورگر خود باز کرده و مقادیر مشخص‌شده را جایگزین کنید:

    ```
    https://api.telegram.org/bot[YOUR_BOT_TOKEN]/setWebhook?url=[YOUR_WORKER_URL]
    ```
-   `[YOUR_BOT_TOKEN]` را با توکن ربات خود جایگزین کنید.
-   `[YOUR_WORKER_URL]` را با آدرس ورکر دیپلوی‌شده خود جایگزین کنید.

ربات شما اکنون فعال است!

---

## 💬 نحوه استفاده

-   **شروع ربات**: دستور `/start` را به ربات ارسال کنید تا پیام خوش‌آمدگویی و راهنما را دریافت کنید.
-   **ساخت یک پست**:
    1.  هر پیامی (متن، عکس و...) را به ربات بفرستید.
    2.  روی آن پیام ریپلای کرده و تنظیمات دکمه‌ها را با فرمت زیر ارسال کنید:

    ```
    @TargetChannelOrUser
    متن دکمه ۱ | https://link-to-website.com
    یک دکمه دیگر | https://another-link.com
    ```
    -   خط اول، چت مقصد است (مثلاً `@my_channel` یا یک آیدی عددی).
    -   هر خط بعدی یک دکمه ایجاد می‌کند. متن و لینک دکمه با `|` از هم جدا می‌شوند.

سپس ربات پیام شما را با دکمه‌های شیشه‌ای جدید به مقصد مشخص‌شده ارسال خواهد کرد! 

  ( ربات باید ادمین کانال مقصد و دسترسی لازم را داشته باشد )

---

## 📄 مجوز (License)

این پروژه متن‌باز است و تحت **مجوز MIT** منتشر شده است.
